export const getLanguage = (lang: string): string => {
 

     if(lang === "shn") {
       return "တႆး"
     }
     if(lang === "mm") {
        return "Burmese"
     }
     if(lang === "en") {
         return "English"
     }
}